# To run unit tests

Open command window on application (root)) folder

### To run all tests

npm test

### To run individual test

npm test -- --grep "EVULN test"

The name only needs to be the first part, enough to make it unique
